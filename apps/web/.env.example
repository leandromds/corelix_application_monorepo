# API base URL — used by src/services/api.ts
# In development: uses the Vite proxy (/api → localhost:8000), no change needed.
# In production: set to your deployed API URL (e.g. https://api.corelix.com/api/v1)
VITE_API_URL=/api/v1
