# Frontend environment variables (Vite)
# Copie como .env.local: cp .env.example .env.local

# URL base da API (em produção, troque pelo domínio real)
# Em dev o proxy do vite.config.ts já encaminha /api/* para localhost:8000
VITE_API_URL=http://localhost:8000
VITE_APP_TITLE=Secretária Digital
