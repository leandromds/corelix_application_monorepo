"""
Reports router — HTTP layer for reports and analytics.

Endpoints to implement:
- GET /reports/sessions     -> session summary report
- GET /reports/revenue      -> revenue report
- GET /reports/clients      -> client activity report
- GET /reports/ai-insights  -> AI-generated insights (uses Claude)
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement endpoints after service layer is ready
